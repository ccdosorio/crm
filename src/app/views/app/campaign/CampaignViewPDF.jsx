import React from "react";
import { Breadcrumb } from "@gull";

const CampaignViewPDF = () => {
  return (
    <div>
      <Breadcrumb routeSegments={[{ name: "PDF" }]} />
    </div>
  );
};

export default CampaignViewPDF;
